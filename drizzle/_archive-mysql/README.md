# Migrations MySQL archivées

Ces migrations correspondent au socle MySQL de la plateforme d'origine.
Elles sont conservées pour référence historique et ne doivent plus être
appliquées : le projet fonctionne désormais sur PostgreSQL (Supabase), dont
les migrations sont générées à la racine de `drizzle/`.
